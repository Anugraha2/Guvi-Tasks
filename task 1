Http 1.1	Http 1.2
Slower than Http 1.2
	Faster than Http 1.1 
Do not use prioritization	Uses prioritization to load a web page and so effective.
It loads resources one after the other, so if one resource cannot be loaded, it blocks all the other resources behind it	It is able to use a single TCP connection to send multiple streams of data at once so that no one resource blocks any other resource.  (Multiplexing)
Will not do server push. Will provide content only if client looks.	Will do server push.
Compresses Http messages but not advanced.	Advanced compression called Hpack that eliminates redundant information.

Difference between Http 1.1 and Http1.2

Write a blog about objects and its internal representation in Javascript
  example 
   var resume = {
    name : "anu",
    profession : "  IT",
   }

it is  a key: value pair
console.log(resume.name))  it will give anu

Javascript Object Notation (JSON)
 for this, the key also should be in "" 

 In JavaScript, almost "everything" is an object

Booleans , it will zero or one , true or false
Numbers can be objects it will return 1,2,3,4,
Strings can be objects , it will be in double quotes or single quotes ,  , 'anu'
Arrays are always objects , it will be like [], what ever inside this symbol, will be in double r single quotes 
Functions  objects , it will be () , arrow functions =>
Objects are always objects  , it will be like { key : value pair }


